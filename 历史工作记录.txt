# 历史工作记录

## 2026-08-26 21:52:10 -04:00

- 用户 Prompt 摘要：根据两张 Windows 高 DPI 截图统一 Elo League Calculator 的图形界面，并更新 GitHub。
- 具体动作：将原生 `simpledialog` 文本与整数输入窗口替换为共用的主题化 `ttk` 输入对话框；统一输入验证、Segoe UI 字体、间距、按钮顺序、居中、模态行为和 Escape/Enter 键行为；新增主题化信息、警告、错误和 Yes/No 确认窗口；让主窗口及 app-owned `Toplevel` 在受支持的 Windows 版本上同步明暗标题栏；让 Edit League Rules 与 Backups 使用共用窗口配置；更新 README 和发行说明；建立 `AGENT_PERSONAL_CUSTOMIZATION.md`；从已提交的 model/storage baseline 隔离构建 v10，避免纳入工作区中与本任务无关的 `elo_model.py` `sb_score` 修改。
- 修改文件：`elo_calculator.py`（主要修改行 216–497、882–928、1137–1840）；`README.md`；`DISTRIBUTION_README.txt`；`AGENT_PERSONAL_CUSTOMIZATION.md`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v10-Windows-x64/README.txt`；`release/EloLeagueCalculator-v10-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v10-Windows-x64.zip`；本文件。
- 测试或验证结果：并发提交 `ad8f784` 加入 SB 功能后，从该精确 `HEAD` 重新构建以保证源码与 exe 一致；`python -m unittest -v` 共 37 项全部通过；Python 编译检查通过；200% Tk scaling 下验证 dark/light 输入窗口、错误窗口、确认窗口、Edit League Rules 和 Backups 的主题、尺寸、控件与按钮；确认窗口默认 No；PyInstaller v10 构建成功；隔离 `%LOCALAPPDATA%` 的可执行程序启动 smoke test 通过并删除测试数据；exe SHA-256 为 `64437C8036D6FCFCB342A613FB4AD2A6F931984BF7719622EC3E457EA4FBC506`；ZIP SHA-256 为 `A56E46E20142E65C50B659780F031EC1BB78B8E6171A1E49C9CF1C98564912B7`。
- GitHub Timeline：当前未 commit；用户已明确要求更新 GitHub，完成本记录后将进行 focused commit 和 push。与本任务无关的 `elo_model.py`、`__pycache__`、`.build_tools` 状态及临时 build 目录不会加入提交。

## 2026-08-26 22:02:00 -04:00

- 用户 Prompt 摘要：将标准化图形界面更新到 GitHub。
- 具体动作：确认并发提交 `ad8f784` 已包含标准化 GUI 源码和 SB 功能；提交 v10 发行文件与项目协议为 `a155416`；将 main 推送至 GitHub；使用 `git ls-remote` 验证 remote main；发现本地 `refs/remotes/origin/main` 文件由 41 个 NUL 字节损坏后，仅删除该精确 tracking ref，并通过 `git fetch origin main` 重建。
- 修改文件：仅 append 更新 `历史工作记录.txt`；前一提交 `a155416` 包含 v10 release、当前 dist exe、发行说明和 `AGENT_PERSONAL_CUSTOMIZATION.md`。
- 测试或验证结果：GitHub remote main 返回 `a155416e368c886534f852e6cdde901a5f86258a`；修复后 `origin/main...HEAD` 为 `0 0`；没有重写本地或远端提交历史。
- GitHub Timeline：`ad8f784` 与 `a155416` 已 push 至 `origin/main`。本条 append-only Timeline 记录将在后续 focused log commit 中提交并 push。

## 2026-08-27 17:10:00 -04:00
# 历史工作记录

## 2026-08-26 21:52:10 -04:00

- 用户 Prompt 摘要：根据两张 Windows 高 DPI 截图统一 Elo League Calculator 的图形界面，并更新 GitHub。
- 具体动作：将原生 `simpledialog` 文本与整数输入窗口替换为共用的主题化 `ttk` 输入对话框；统一输入验证、Segoe UI 字体、间距、按钮顺序、居中、模态行为和 Escape/Enter 键行为；新增主题化信息、警告、错误和 Yes/No 确认窗口；让主窗口及 app-owned `Toplevel` 在受支持的 Windows 版本上同步明暗标题栏；让 Edit League Rules 与 Backups 使用共用窗口配置；更新 README 和发行说明；建立 `AGENT_PERSONAL_CUSTOMIZATION.md`；从已提交的 model/storage baseline 隔离构建 v10，避免纳入工作区中与本任务无关的 `elo_model.py` `sb_score` 修改。
- 修改文件：`elo_calculator.py`（主要修改行 216–497、882–928、1137–1840）；`README.md`；`DISTRIBUTION_README.txt`；`AGENT_PERSONAL_CUSTOMIZATION.md`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v10-Windows-x64/README.txt`；`release/EloLeagueCalculator-v10-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v10-Windows-x64.zip`；本文件。
- 测试或验证结果：并发提交 `ad8f784` 加入 SB 功能后，从该精确 `HEAD` 重新构建以保证源码与 exe 一致；`python -m unittest -v` 共 37 项全部通过；Python 编译检查通过；200% Tk scaling 下验证 dark/light 输入窗口、错误窗口、确认窗口、Edit League Rules 和 Backups 的主题、尺寸、控件与按钮；确认窗口默认 No；PyInstaller v10 构建成功；隔离 `%LOCALAPPDATA%` 的可执行程序启动 smoke test 通过并删除测试数据；exe SHA-256 为 `64437C8036D6FCFCB342A613FB4AD2A6F931984BF7719622EC3E457EA4FBC506`；ZIP SHA-256 为 `A56E46E20142E65C50B659780F031EC1BB78B8E6171A1E49C9CF1C98564912B7`。
- GitHub Timeline：当前未 commit；用户已明确要求更新 GitHub，完成本记录后将进行 focused commit 和 push。与本任务无关的 `elo_model.py`、`__pycache__`、`.build_tools` 状态及临时 build 目录不会加入提交。

## 2026-08-26 22:02:00 -04:00

- 用户 Prompt 摘要：将标准化图形界面更新到 GitHub。
- 具体动作：确认并发提交 `ad8f784` 已包含标准化 GUI 源码和 SB 功能；提交 v10 发行文件与项目协议为 `a155416`；将 main 推送至 GitHub；使用 `git ls-remote` 验证 remote main；发现本地 `refs/remotes/origin/main` 文件由 41 个 NUL 字节损坏后，仅删除该精确 tracking ref，并通过 `git fetch origin main` 重建。
- 修改文件：仅 append 更新 `历史工作记录.txt`；前一提交 `a155416` 包含 v10 release、当前 dist exe、发行说明和 `AGENT_PERSONAL_CUSTOMIZATION.md`。
- 测试或验证结果：GitHub remote main 返回 `a155416e368c886534f852e6cdde901a5f86258a`；修复后 `origin/main...HEAD` 为 `0 0`；没有重写本地或远端提交历史。
- GitHub Timeline：`ad8f784` 与 `a155416` 已 push 至 `origin/main`。本条 append-only Timeline 记录将在后续 focused log commit 中提交并 push。

## 2026-08-27 17:10:00 -04:00

- 用户 Prompt 摘要：打包发布 11v 版本。
- 具体动作：通过 PyInstaller 生成了 EloLeagueCalculator.exe；将可执行文件和 DISTRIBUTION_README.txt 打包为 `release/EloLeagueCalculator-v11-Windows-x64.zip`。
- 修改文件：`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v11-Windows-x64/README.txt`；`release/EloLeagueCalculator-v11-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v11-Windows-x64.zip`；本文件。
- 测试或验证结果：PyInstaller 构建成功，zip 压缩文件已生成并准备完毕。

## 2026-08-28 17:06:00 -04:00

- 用户 Prompt 摘要：为活动日志添加横向滚动条并打包发布 v12 版本。
- 具体动作：在 `elo_calculator.py` 中的 `Activity log` 树视图底部添加了一个横向滚动条以防止长文本被截断。随后使用 PyInstaller 构建了可执行程序，并将其打包至 `release/EloLeagueCalculator-v12-Windows-x64.zip`。
- 修改文件：`elo_calculator.py`；`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v12-Windows-x64/README.txt`；`release/EloLeagueCalculator-v12-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v12-Windows-x64.zip`；本文件。
- 测试或验证结果：UI 更新通过语法测试，PyInstaller 成功将其构建为独立应用包。

## 2026-08-30 21:02:00 -04:00

- 用户 Prompt 摘要：在活动日志中添加 SB 分数并打包发布 v12 版本。
- 具体动作：更新了 `elo_calculator.py`，在记录和撤销比赛的活动日志字符串中追加了相关玩家的 SB 分数。由于 v12 正在使用中，为了避免文件冲突，直接发布了 `release/EloLeagueCalculator-v13-Windows-x64.zip`。
- 修改文件：`elo_calculator.py`；`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v13-Windows-x64/README.txt`；`release/EloLeagueCalculator-v13-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v13-Windows-x64.zip`；本文件。
- 测试或验证结果：更新后的 `elo_calculator.py` 编译通过，PyInstaller 构建并打包成功。

## 2026-08-30 21:09:00 -04:00

- 用户 Prompt 摘要：将按钮重命名为 settings。
- 具体动作：为了提高应用清晰度，将 `elo_calculator.py` 中的主界面 "Edit Rules" 按钮文本修改为 "Settings"，并将其对应的对话框标题由 "Edit League Rules" 修改为 "League Settings"。
- 修改文件：`elo_calculator.py`；`historyworklog.txt`；本文件。
- 测试或验证结果：通过 Python 编译检查，重命名生效。

## 2026-08-30 21:11:00 -04:00

- 用户 Prompt 摘要：build。
- 具体动作：通过 PyInstaller 重新构建了可执行程序以包含 Settings 命名的更改，并将新生成的可执行文件打包至 `release/EloLeagueCalculator-v14-Windows-x64.zip`。
- 修改文件：`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v14-Windows-x64/README.txt`；`release/EloLeagueCalculator-v14-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v14-Windows-x64.zip`；本文件。
- 测试或验证结果：PyInstaller 构建并打包 v14 成功。

## 2026-08-30 21:14:00 -04:00

- 用户 Prompt 摘要：update readme。
- 具体动作：更新了 `README.md`，区分了全局主题 Settings 菜单与具体的联赛 Settings 按钮（原 Edit Rules）；并补充说明了活动日志现在支持横向滚动条并包含 SB 分数记录。
- 修改文件：`README.md`；`historyworklog.txt`；本文件。
- 测试或验证结果：文档更新完成。

## 2026-08-30 21:13:00 -04:00

- User Prompt summary: Make the K-factor and Elo rounding customizable, use English, and push the update to GitHub.
- Actions: Added per-league K-factor and Elo decimal-place controls to Edit League Rules; validated K from 0.01 through 1000 and rounding from 0 through 6 decimal places; persisted both settings in schema v6; stored the settings used by every match so roster replay preserves historical Elo calculations; standardized rating formatting across standings, preview, history, audit text, status text, and reset text; updated documentation; rebuilt the tracked Windows executable.
- Modified files: `elo_model.py`; `elo_calculator.py`; `test_elo_model.py`; `README.md`; `DISTRIBUTION_README.txt`; `dist/EloLeagueCalculator.exe`; append-only update to `历史工作记录.txt`.
- Tests and verification: All 42 unittest cases passed before and after rebasing onto the newer remote Settings/SB commits; the League Settings GUI smoke check found K-factor, Elo decimal-place, and Save controls; the final merged executable remained running during a three-second isolated launch test; executable SHA-256 is `8AE5DDD03B3ABA2401A08176D124550FAAEF5D7147F7F247E51DA03E80039836`. The first PyInstaller attempt encountered a locked stale `build/EloLeagueCalculator/localpycs` directory, so successful builds used fresh isolated directories; temporary build directories were removed, and only intended generated artifacts remain modified.
- GitHub Timeline: Not committed or pushed yet. A focused feature/build commit and push to `origin/main` will follow this record.

## 2026-08-30 21:26:00 -04:00

- User Prompt summary: Confirm completion of the customizable K-factor and Elo-rounding GitHub update.
- Actions: Rebased the feature onto concurrent remote commits for SB activity details, Settings naming, v14 packaging, and README improvements; resolved conflicts by preserving all concurrent features; repaired corrupted dash and plus/minus characters from the concurrent source; rebuilt and launch-tested the merged executable; pushed the feature commit.
- Modified files: Append-only update to `历史工作记录.txt`; feature commit `a29d403` contains the seven implementation, test, documentation, executable, and work-log files recorded above.
- Tests and verification: 42/42 unittest cases passed after integration; the final merged executable passed the isolated launch test; `git push origin main` advanced the remote from `c5b89db` to `a29d403`.
- GitHub Timeline: Feature commit `a29d403` has been pushed to `origin/main`. This final confirmation record will be committed and pushed as a focused log-only follow-up.

## 2026-08-30 21:32:00 -04:00

- User Prompt summary: Review the code, fix bugs, use English, commit, and update GitHub.
- Actions: Because Windows reports the official OneDrive checkout as corrupted and unreadable, created a temporary local Git clone from the latest `origin/main` commit `c15a3d1`; preserved the concurrent customizable K-factor and Elo-rounding schema-v6 feature; rejected nonfinite Elo transfers and resulting ratings before any league mutation; removed the shadowed duplicate `_build_ui`; removed the hidden UTF-8 BOM left in `elo_calculator.py`; added an overflow regression test; synchronized league Settings terminology in the distribution guide; rebuilt the current Tcl/Tk Windows executable; created the v15 release directory and ZIP.
- Modified files: `elo_model.py`; `elo_calculator.py`; `test_elo_model.py`; `DISTRIBUTION_README.txt`; `dist/EloLeagueCalculator.exe`; `release/EloLeagueCalculator-v15-Windows-x64/README.txt`; `release/EloLeagueCalculator-v15-Windows-x64/EloLeagueCalculator.exe`; `release/EloLeagueCalculator-v15-Windows-x64.zip`; `historyworklog.txt`; append-only update to this file.
- Tests and verification: All 43 unittest cases passed; direct UTF-8 source compilation passed; AST duplicate-method scan returned an empty list; BOM/mojibake scan returned no matches; PyInstaller 6.22.0 included 918 GUI runtime entries and `pyi_rth__tkinter.py`; the executable remained running during a five-second isolated `%LOCALAPPDATA%` launch test; executable SHA-256 is `FF13017ADD5E0B81537E818DC54C7688FB4001E7F0986B2E9DFFF1EA3C3E7BDE`; ZIP SHA-256 is `C233178F0CD3929071279A589A14ED223A523E68078B6F9A392069BFAE3A3CC3`.
- GitHub Timeline: Not committed or pushed yet. The user explicitly authorized commit and push. A focused commit and fast-forward push to `origin/main` will follow; temporary PyInstaller hooks, build intermediates, and pycache are outside the repository and will not be committed. The official `C:\Users\danie\OneDrive\Documents\ChatGPT\elo` checkout remains corrupted and cannot be synchronized locally.

## 2026-08-30 21:35:00 -04:00

- User Prompt summary: Confirm the reviewed bug fixes and GitHub update.
- Actions: Created focused commit `5d22cea` from latest remote commit `c15a3d1` and pushed it to `origin/main`; the commit includes the nonfinite Elo guard, duplicate UI cleanup, BOM cleanup, 43-test regression suite, current executable, and v15 package. Configured the existing repository author identity only inside the temporary clone because it initially had no local author configuration.
- Modified files: Append-only update to this file; implementation and release files are contained in commit `5d22cea`.
- Tests and verification: GitHub push advanced remote main from `c15a3d1` to `5d22cea`; no force push or history rewrite was used. All validation and hashes remain those recorded in the preceding 21:32 entry.
- GitHub Timeline: Commit `5d22cea` is pushed to `origin/main`. This post-push confirmation entry will be committed and pushed immediately as a focused log-only follow-up. The official OneDrive checkout remains corrupted and unreadable.

## 2026-08-31 19:50:00 -04:00

- User Prompt summary: Review the code and commit the fixes.
- Actions: Reviewed the latest synchronized `origin/main` from temporary local commit `8a51d06` because the official OneDrive checkout remains corrupted; made roster reduction replay transactional so an invalid retained match cannot partially shrink or reset the league; rejected nonfinite and nonnumeric inputs in `expected_score`; made `LeagueCollection.from_dict` return its documented `ValueError` for non-object input; guarded the live League Settings multiplier editor against typed match targets outside 1–100; added regression tests; rebuilt the tracked Windows executable and updated its distribution checksum.
- Modified files: `elo_model.py`; `elo_storage.py`; `elo_calculator.py`; `test_elo_model.py`; `DISTRIBUTION_README.txt`; `dist/EloLeagueCalculator.exe`; `historyworklog.txt`; append-only update to this file.
- Tests and verification: All 46 unittest cases passed; direct UTF-8 source compilation passed; `git diff --check` passed; PyInstaller 6.22.0 included 918 GUI runtime entries and `pyi_rth__tkinter.py`; the rebuilt executable remained running during a five-second isolated `%LOCALAPPDATA%` launch test; executable SHA-256 is `14F7913CCD793F23E05A71ED3FD86060D7B27889F5EAEECD7325AC80273913B4`.
- GitHub Timeline: Not committed at the time of this entry. The user authorized a local commit but did not request a push; a focused commit will follow and remain unpushed. Temporary PyInstaller inputs and generated pycache will not be committed. The official `C:\Users\danie\OneDrive\Documents\ChatGPT\elo` checkout remains corrupted and unreadable.

## 2026-08-31 19:45:00 -04:00

- User Prompt summary: Add a league simulator for First to N.
- Actions: Added a read-only Monte Carlo single round-robin engine in `elo_simulator.py`; every player meets once per simulated season; game probabilities use current Elo; match order is randomized; simulated rating changes use the active league's K-factor, decimal rounding, First-to-N score multipliers, and auto-Elo setting; aggregate output includes title probability, average rank, average match W-L, and average game W-L. Added a themed Simulator dialog with run-count limits, optional reproducible random seed, scrollable results, custom-score rejection, and no persistence changes. Workload limits account for roster size and First-to-N target. Updated documentation and rebuilt the tracked executable.
- Modified files: `elo_simulator.py`; `elo_calculator.py`; `test_elo_model.py`; `README.md`; `DISTRIBUTION_README.txt`; `dist/EloLeagueCalculator.exe`; append-only update to `历史工作记录.txt`.
- Tests and verification: 44/44 unittest cases passed before integration with the concurrent validation fixes; seeded results are reproducible; the simulator leaves league serialization unchanged; Tkinter smoke test ran 25 seasons and displayed all 12 players; packaged executable passed an isolated three-second launch test. The executable will be rebuilt again after integration.
- GitHub Timeline: Simulator commit `461a053` was local and unpushed when the user requested GitHub publication. Rebase onto `origin/main` validation commits is in progress; no force push will be used.

## 2026-08-31 20:02:00 -04:00

- User Prompt summary: Publish the First-to-N league simulator to GitHub.
- Actions: Fetched three concurrent validation/replay commits; rebased the simulator onto `origin/main`; preserved nonfinite Elo validation, transactional roster replay, collection-shape validation, live Settings bounds, and the cleaned single `_build_ui`; reapplied only the simulator integration; rebuilt the executable from the combined source.
- Modified files: `elo_simulator.py`; `elo_calculator.py`; `test_elo_model.py`; `README.md`; `DISTRIBUTION_README.txt`; `dist/EloLeagueCalculator.exe`; append-only update to `历史工作记录.txt`.
- Tests and verification: Combined suite passed 48/48 tests; PyInstaller build succeeded; the merged executable passed an isolated three-second launch test and all temporary executable processes were stopped by verified path. Executable SHA-256 is `C74EE2F3F87811C3D3C507A83596521558AD0EDEC0161AFE8D36F3CF2EC495C0`.
- GitHub Timeline: Rebased simulator commit exists locally and is not yet pushed. The user explicitly requested GitHub publication; the final binary/checksum will be amended into the commit and pushed without force.
## 2026-08-31 19:44:00 -04:00

- 用户 Prompt 摘要：Add a draw option，并要求使用 English。
- 具体动作：在数据模型中加入正式的 draw result、标准 Elo 0.5 实际得分计算、W-D-L 统计、draw-aware match percentage、Sonneborn-Berger 平局计分、撤销、roster replay 和 schema 5 向后兼容；在桌面界面加入 `Record draw` 按钮、draw Elo preview、W-D-L standings、history/activity/status 和 undo 文案；更新 README；增加 draw regression tests。
- 修改文件：`elo_model.py`；`elo_calculator.py`；`test_elo_model.py`；`README.md`；`historyworklog.txt`；本文件。
- 测试或验证结果：Python compile 检查通过；43 项 unittest 全部通过。尝试使用 bundled workspace Python 做 Tk GUI smoke test，但该 runtime 缺少可用的 `tcl8.6/init.tcl`，Tk root 无法启动，因此没有完成 live GUI rendering 验证；静态 UI integration 和 model-backed action 已检查。
- GitHub Timeline：未 commit，未 push；用户本次没有要求 commit 或 push。工作区已有与本任务无关的 graph UI、build/release、`.build_tools` 和 cache 改动，均未清理或提交。

## 2026-08-31 20:00:00 -04:00

- 用户 Prompt 摘要：GitHub。
- 具体动作：验证 43 项 unittest 和 Python compile；仅暂存 draw feature 的 6 个 source/documentation/history 文件，排除 build、release、cache、helper scripts 和 `.build_tools` 删除；创建 commit `13e1526`。确认此前未 push 的 Graphs commit 为 `de142aa`。
- 修改文件：仅 append 更新 `historyworklog.txt` 和本文件；draw feature 文件已包含于 `13e1526`。
- 测试或验证结果：43 项 unittest 全部通过，Python compile 通过；focused staged diff 无 whitespace error。
- GitHub Timeline：已经执行 `git push` 将包括 Draw 功能和 Graphs 功能在内的所有本地 commit 推送至 `origin/main`。

## 2026-08-31 20:04:00 -04:00

- 用户 Prompt 摘要：generate package for most recent version。
- 具体动作：通过 PyInstaller 重新构建了可执行程序，并将新生成的可执行文件打包至 `release/EloLeagueCalculator-v15-Windows-x64.zip`。
- 修改文件：`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v15-Windows-x64/README.txt`；`release/EloLeagueCalculator-v15-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v15-Windows-x64.zip`；本文件。
- 测试或验证结果：PyInstaller 构建并打包 v15 成功。

## 2026-09-01 20:12:00 -04:00

- 用户 Prompt 摘要：Add a league simulator for First to N，并将合并后的项目更新到 GitHub。
- 具体动作：以普通 merge 方式合并远端 Graphs/draw result 分支和本地 simulator、customizable K-factor/rounding、validation 更新，不使用 force push；解决 model、GUI、tests、documentation 和 append-only logs 冲突；draw Elo 与显示现在使用各 league 的 K-factor 和 decimal precision；Graphs 的 match percentage 和 SB calculation 支持 draws；保留 transactional historical replay；增加 regression test；bundled runtime 自动检测 Tk 失败后，增加可复现的 PyInstaller Tcl/Tk hooks；重新构建 tracked executable。
- 修改文件：`elo_model.py`；`elo_calculator.py`；`elo_simulator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`EloLeagueCalculator.spec`；`packaging_hooks/hook-_tkinter.py`；`packaging_hooks/pre_find_module_path/hook-tkinter.py`；`dist/EloLeagueCalculator.exe`；`historyworklog.txt`；本文件。
- 测试或验证结果：55 项 unittest 全部通过；conflict-marker 和 whitespace 检查通过；最终 PyInstaller build classified 1,003 runtime entries，并包含 `pyi_rth__tkinter.py`；executable 在 isolated five-second launch test 中持续运行；SHA-256 为 `EAF19DF9FC2C9EC945C5F8A19C62FA6D4677D061E7A971210CEE92D91C665D82`。
- GitHub Timeline：merge 已在本地解决，尚未 commit 或 push。用户明确要求发布到 GitHub；后续将创建 normal merge commit 并 push 到 `origin/main`，不使用 force push。
## 2026-08-31 20:16:00 -04:00

- 用户 Prompt 摘要：simulator button not showing up (并在随后的截图中提示重建 First-to-N league simulator)。
- 具体动作：由于原 simulator 代码在服务器重启中丢失，重新编写了基于 Monte Carlo 的 `LeagueSimulator`，实现了全量循环赛的模拟和结果追踪（Title probability, Avg Rank, Match W-L, Game W-L），在 UI 中添加了 Simulator 按钮和弹窗。增加了对应的 `TestSimulator`。
- 修改文件：`elo_model.py`；`elo_calculator.py`；`test_elo_model.py`；`historyworklog.txt`；本文件。
- 测试或验证结果：包含新测试在内的 44 项 unittest 全部通过。
- GitHub Timeline：仅在本地提交 (commit)，未推送 (push)。

## 2026-09-01 20:00:00 -04:00

- 用户 Prompt 摘要：push and build package。
- 具体动作：将 Simulator 功能推送到了 GitHub；使用 PyInstaller 重新构建可执行程序，并打包至 `release/EloLeagueCalculator-v16-Windows-x64.zip`。
- 修改文件：`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v16-Windows-x64/README.txt`；`release/EloLeagueCalculator-v16-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v16-Windows-x64.zip`；本文件。
- 测试或验证结果：PyInstaller 构建并打包 v16 成功。
- GitHub Timeline：推送（push）成功，并在之后将日志更改也推送到 `origin/main`。

## 2026-09-01 20:04:00 -04:00

- 用户 Prompt 摘要：Add option to disable draws。
- 具体动作：新增每个 league 独立的 `allow_draws` 设置；在 League Settings 增加 `Allow draws` checkbox；关闭时禁用 `Record draw` 和 draw preview，但保留 `Record win`；model 层拒绝新增 draw；历史 draw、W-D-L 和 SB 统计保持不变；保存 schema 升级至 7，schema 1-6 缺少此字段时默认允许 draw；README 增加说明。
- 修改文件：`elo_model.py`；`elo_calculator.py`；`test_elo_model.py`；`README.md`；`historyworklog.txt`；本文件。
- 测试或验证结果：Python compile 通过；48 项 unittest 全部通过，包括 model enforcement、历史 draw 保留、schema-6 migration、strict boolean validation 和 UI button-state regression。
- GitHub Timeline：未 commit，未 push；用户本次未要求 GitHub 更新。既有 build、release、cache 和 helper-script 工作区状态未纳入本次修改。

## 2026-09-01 20:06:00 -04:00

- 用户 Prompt 摘要：Push。
- 具体动作：仅暂存 `allow_draws` 功能对应的 source、test、README 和两个 append-only work log 文件；排除 build、dist、release、cache 和 helper scripts；创建 commit `a0200d6` 并推送至 `origin/main`。
- 修改文件：`README.md`；`elo_calculator.py`；`elo_model.py`；`test_elo_model.py`；`historyworklog.txt`；本文件。
- 测试或验证结果：push 前 Python compile 通过，48 项 unittest 全部通过；focused diff 无 whitespace error；GitHub 接受 fast-forward push `16668cf..a0200d6`。
- GitHub Timeline：`a0200d6` 已 push 至 `origin/main`。本条 publication record 将作为 focused log commit 推送。

## 2026-09-01 20:11:00 -04:00

- 用户 Prompt 摘要：make simulator show w-l and game results -> rebuild and push。
- 具体动作：将 Simulator UI 从显示百分比修改为显示具体的绝对平均 W-L 场次（例如 `2.1 - 0.9`）。修改并提交了该功能后，重新使用 PyInstaller 构建了可执行程序，并打包至 `release/EloLeagueCalculator-v17-Windows-x64.zip`。
- 修改文件：`elo_model.py`；`elo_calculator.py`；`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v17-Windows-x64/README.txt`；`release/EloLeagueCalculator-v17-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v17-Windows-x64.zip`；本文件。
- 测试或验证结果：PyInstaller 构建并打包 v17 成功，48 项单元测试通过。
- GitHub Timeline：推送（push）成功，并在之后将日志更改也推送到 `origin/main`。

## 2026-09-01 20:15:00 -04:00

- 用户 Prompt 摘要：fix the weird characters and commit。
- 具体动作：修复了 UI 中乱码的编码问题（如标题和预览文本中的 `â€“` 和 `Â±` 替换为了正常字符 `-` 和 `+/-`）。
- 修改文件：`elo_calculator.py`；`historyworklog.txt`；本文件。
- 测试或验证结果：乱码字符已被成功替换。
- GitHub Timeline：仅在本地提交 (commit)，未推送 (push)。

## 2026-09-01 20:17:00 -04:00

- 用户 Prompt 摘要：yes (rebuild and push)。
- 具体动作：将编码修复推送到 GitHub。重新使用 PyInstaller 构建了可执行程序，并打包至 `release/EloLeagueCalculator-v18-Windows-x64.zip`。
- 修改文件：`historyworklog.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v18-Windows-x64/README.txt`；`release/EloLeagueCalculator-v18-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v18-Windows-x64.zip`；本文件。
- 测试或验证结果：PyInstaller 构建并打包 v18 成功。
- GitHub Timeline：推送（push）成功，并在之后将日志更改也推送到 `origin/main`。

## 2026-09-02 17:18:00 -04:00

- 用户 Prompt 摘要：继续将合并后的 First-to-N simulator 更新发布到 GitHub。
- 具体动作：第一次 push 因 `origin/main` 新增 commits 被拒绝后，fetch 并以 normal merge 合并九个远端 commits；将 per-league Allow draws policy 和 ASCII-safe UI text 与 customizable K-factor/rounding 集成；保留经过验证、显示 absolute average match/game W-L 的 standalone simulator；删除远端重新引入的 duplicate simulator class 和 shadowing duplicate UI method；修复 League Settings options row 与 Elo settings 重叠；使用 project Tcl/Tk hooks 重新构建 executable。
- 修改文件：`elo_model.py`；`elo_calculator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`historyworklog.txt`；本文件。
- 测试或验证结果：59 项 unittest 全部通过；conflict-marker、mojibake-source 和 whitespace 检查通过；PyInstaller classified 1,003 runtime entries，并包含 `pyi_rth__tkinter.py`；executable 在 isolated five-second launch test 中持续运行；已按 exact executable path 停止验证产生的 child process，并删除 temporary build directories；SHA-256 为 `07F0D5A6206E3A016534870CAC6ECE4FDBA0C981F06AE136C0A8364BABE1978E`。
- GitHub Timeline：latest `origin/main` 已在本地完成 conflict resolution，尚未 commit 或 push；后续将使用 normal merge commit 和 push，不重写 remote history。

## 2026-09-02 17:20:00 -04:00

- 用户 Prompt 摘要：确认继续执行的 GitHub publication。
- 具体动作：整合全部 concurrent remote work 后创建 merge commit `8ec116a`，并推送包含 simulator、draw policy、configurable Elo、graphs、validation、packaging、documentation、tests 和 executable 的完整更新。
- 修改文件：append-only 更新 `historyworklog.txt` 和本文件；implementation 和 executable changes 位于 merge commits `b27b5ef` 和 `8ec116a`。
- 测试或验证结果：GitHub 接受 normal push `0000725..8ec116a`；push 后本地 `HEAD` 与 `origin/main` 均为 `8ec116aa3ca3635bb3fd991aa31f40664ddc5b03`。最终验证仍为 59/59 tests passed，executable SHA-256 为 `07F0D5A6206E3A016534870CAC6ECE4FDBA0C981F06AE136C0A8364BABE1978E`。
- GitHub Timeline：merge commit `8ec116a` 已 push 至 `origin/main`；本条 final publication record 将作为 log-only follow-up commit 并 push。

## 2026-09-03 08:17:00 -04:00

- 用户 Prompt 摘要：修复 screenshot 中 league heading 显示的 `â€”` 类乱码 separator。
- 具体动作：定位到 `_refresh_league_selector` 中剩余的两个 Unicode em dash；将 heading 和 window title separators 替换为 ASCII-safe ` - `；增加两个 exact strings 的 regression test；扫描全部 Python source 确认没有 non-ASCII characters；重新构建 tracked Windows executable。
- 修改文件：`elo_calculator.py`；`test_elo_model.py`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`historyworklog.txt`；本文件。
- 测试或验证结果：60 项 unittest 全部通过；全部 Python source files 通过 ASCII-only scan；`git diff --check` 通过；PyInstaller classified 1,003 runtime entries，并包含 `pyi_rth__tkinter.py`；executable 在 isolated five-second launch test 中持续运行；SHA-256 为 `4E87C4614FFCE9B6799B5815E714538F6801CA980AC754566907D49A8CFF52C0`。Read-only process inspection 表明 screenshot 来自 `C:\Code\elo_calculator` 下的 older v18 executable；没有停止或覆盖这些 user-owned processes。
- GitHub Timeline：尚未 commit 或 push。用户要求 fix，但没有要求 GitHub push；后续将创建 focused local commit。
## 2026-09-02 17:25:47 -04:00

- 用户 Prompt 摘要：review and push；continue。
- 具体动作：由于 GitHub 在两次最终 push 检查前分别新增 4、3 和 9 个并发 commit，未覆盖远端历史；改为从最新远端 `8c870bb` 创建新的临时 clone 并重新审查。确认最新分支已合并此前发现的 rating validation、atomic replay、malformed storage、duplicate UI、draw policy、configurable Elo 和 standalone simulator 修复。进一步复现并修复剩余 Simulator 公平性问题：当所有 standings tiebreaker 完全相同时，原实现按 player ID 将 100% title probability 给第一个 player 并分配不同名次；现在并列者平均分享 title probability 和 rank credit。更新 README、分发说明、回归测试和 Windows executable。
- 修改文件：`elo_simulator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`historyworklog.txt`；本文件。
- 测试或验证结果：最新架构全部 60 项 unittest 通过；三人完全并列回归由 `[(100%, 1), (0%, 2), (0%, 3)]` 修复为每人 `33.333...%`、平均 rank `2.0`；`git diff --check` 通过；PyInstaller 6.22.0 收集 1,003 项 runtime entries 并包含 Tk runtime hook；新 EXE 在隔离 `LOCALAPPDATA` 中运行 5 秒未退出，所有精确路径匹配的 smoke-test 进程均已停止。EXE SHA-256 为 `891F00637A324891BAE5B47030AC1E0D7ABC2494ECEB9DC666A725609E75EFC9`。
- GitHub Timeline：最新 `origin/main` 为 `8c870bb`；当前修复尚未 commit 或 push，用户已授权，最终远端检查后将执行 normal fast-forward push。

## 2026-09-02 17:27:25 -04:00

- 用户 Prompt 摘要：完成 review and push。
- 具体动作：创建 focused commit `805bb0f`，包含 Simulator 完全并列公平性修复、回归测试、文档、重建 executable 和前一条审查记录；push 前 fetch 确认相对 `origin/main` 为 ahead 1 / behind 0，随后执行 normal fast-forward push。
- 修改文件：仅 append 更新 `historyworklog.txt` 和本文件；实现与 executable 文件包含在 `805bb0f`。
- 测试或验证结果：发布验证保持为 60/60 tests passed；EXE isolated smoke test 通过；SHA-256 为 `891F00637A324891BAE5B47030AC1E0D7ABC2494ECEB9DC666A725609E75EFC9`；`git ls-remote` 返回 `805bb0fd1af9ddf2bde0d4ddde93446b1f71c731`。
- GitHub Timeline：commit `805bb0f` 已成功 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 推送。

## 2026-09-03 08:20:00 -04:00

- 用户 Prompt 摘要：将 ASCII-safe league-heading fix push 到 GitHub。
- 具体动作：initial push 因 `origin/main` 新增 simulator tied-standings fairness update 被拒绝；fetch 并以 normal merge 合并 commits `805bb0f` 和 `a76a43f`；同时保留 heading fix 和 shared title/rank credit；合并两个 regression tests；从 merged source 重新构建 tracked executable。
- 修改文件：`elo_calculator.py`；`elo_simulator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`historyworklog.txt`；本文件。
- 测试或验证结果：61 项 unittest 全部通过；Python source 保持 ASCII-only；conflict-marker 和 whitespace checks 通过；PyInstaller classified 1,003 runtime entries，并包含 `pyi_rth__tkinter.py`；executable 通过 isolated five-second launch test；temporary process 和 build directories 已按 verified path 清理；SHA-256 为 `981EABEBC09E5D7F76292E5C0ABE804790688A56E9CD2E1CBA6F10DC2C37942A`。
- GitHub Timeline：latest remote work 已在本地完成 merge，尚未 commit 或 push；后续将使用 normal merge commit 和 push，不使用 force。

## 2026-09-03 08:20:44 -04:00

- 用户 Prompt 摘要：完成要求的 GitHub push。
- 具体动作：创建 merge commit `2fbfb39`，包含 ASCII-safe heading fix、simulator tied-standings fairness update、combined tests、documentation、work logs 和 rebuilt executable；以 normal push 更新 `origin/main`。
- 修改文件：append-only 更新 `historyworklog.txt` 和本文件；implementation 和 executable changes 位于 `2fbfb39`。
- 测试或验证结果：GitHub 接受 push `a76a43f..2fbfb39`；本地 `HEAD` 与 `origin/main` 均为 `2fbfb39a3e0567c157f4a5e0bff4a63f5a5d0a27`；最终验证保持 61/61 tests passed，executable SHA-256 为 `981EABEBC09E5D7F76292E5C0ABE804790688A56E9CD2E1CBA6F10DC2C37942A`。
- GitHub Timeline：merge commit `2fbfb39` 已 push 至 `origin/main`；本条 final record 将作为 log-only follow-up commit 并 push。

## 2026-09-03 08:34:00 -04:00

- 用户 Prompt 摘要：使 simulator results 可以更新 leagues、使 graphs 兼容 dark mode，并 push 完成的更新。
- 具体动作：新增可复现的 First-to-N 单轮循环赛具体结果和 **Apply One Season** 操作，通过正常 league persistence、automatic backup 和 audit 流程记录全部模拟比赛；图表 canvas、axis、grid、label 和 plot 颜色全部改用当前主题调色板；在 concurrent simulator 和 heading updates 之后基于最新 `origin/main` 完成集成。
- 修改文件：`elo_simulator.py`；`elo_calculator.py`；`test_elo_model.py`；`README.md`；`historyworklog.txt`；本文件。
- 测试或验证结果：integrated branch 的最终 compile、unittest、conflict-marker 和 whitespace checks 待执行。
- GitHub Timeline：changes 已集成至基于当前 `origin/main` 的本地 branch `codex/simulator-dark-mode`；尚未 commit 或 push；后续使用 normal fast-forward push，不使用 force。

## 2026-09-03 08:34:56 -04:00

- 用户 Prompt 摘要：push simulator league-update 和 dark-mode graph changes。
- 具体动作：基于 latest remote code 创建 focused commit `2d238ef` 并以 normal push 更新 `origin/main`；保留 concurrent simulator tie handling 和 heading fixes；generated bytecode 与 root worktree 中无关 artifacts 均未纳入提交。
- 修改文件：append-only 更新 `historyworklog.txt` 和本文件；implementation、tests 和 README changes 位于 `2d238ef`。
- 测试或验证结果：64 项 unit tests 全部通过；Python compilation、conflict-marker check 和 whitespace check 均通过；push 后 local integration `HEAD` 与 `origin/main` 均为 `2d238ef6ceb34c843c1160f2f44d4c8dc44a7e4d`。
- GitHub Timeline：commit `2d238ef` 已成功 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 推送。

## 2026-09-03 08:36:36 -04:00

- 用户 Prompt 摘要：review code fix and commit。
- 具体动作：在本地 OneDrive Git repository 的最新 `main` (`35e4629`) 上审查 model、storage、simulator 和 UI。复现 Graphs tab 的 SB 历史错误：Player 1 击败 Player 2 后，Player 2 后续获胜会提高 Player 1 的当前 SB，但 graph 因只在 selected player 参赛时采样而保持旧值。将 SB timeline 改为每场 league match 后重新计算并追加；增加 mock-canvas regression test；更新 README、分发说明和 tracked Windows executable。
- 修改文件：`elo_calculator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`historyworklog.txt`；本文件。
- 测试或验证结果：62 项 unittest 全部通过；`git diff --check` 通过；全部 Python source 的 AST duplicate-method 和 ASCII-only 检查通过；PyInstaller 6.22.0 classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；EXE 在 isolated `LOCALAPPDATA` 下运行 5 秒未退出，两个精确路径匹配进程均停止。EXE SHA-256 为 `12B8A351BCD324525C36D5EBFE07C38AC211DD169DC4862B3370526ACDDE5AB5`。
- GitHub Timeline：implementation commit `22e3d06` 已在本地创建，尚未 push；用户要求 commit，未要求 push。本 publication record 将作为本地 log-only follow-up commit 提交。

## 2026-09-03 08:41:37 -04:00

- 用户 Prompt 摘要：push。
- 具体动作：push 前 fetch 发现 `origin/main` 新增 commits `2d238ef` 和 `93662e0`；使用 normal merge 保留新增的 Apply One Season 和 dark-mode graph 功能，同时保留 opponent-driven SB graph 修复；合并 tests、README 和两份 append-only work log；从最终 merged source 重新构建 executable。
- 修改文件：`elo_calculator.py`；`elo_simulator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`historyworklog.txt`；本文件。
- 测试或验证结果：最终 merged suite 的 65 项 unittest 全部通过；Python AST、conflict-marker 和 `git diff --check` 检查通过；PyInstaller 6.22.0 classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；合并后 EXE 在 isolated `LOCALAPPDATA` 中运行 5 秒未退出并停止全部精确路径匹配进程。最终 EXE SHA-256 为 `6184478E2D626B03B2F7311F2A93BD5D75519696339CF47DDCDFC86CD72A7F34`。
- GitHub Timeline：本地 commits `22e3d06`、`136608a` 已与最新 `origin/main` 正常合并，merge commit 尚未创建或 push；用户已明确授权 push，将完成 merge commit 后 normal push。

## 2026-09-03 08:42:44 -04:00

- 用户 Prompt 摘要：完成 push。
- 具体动作：创建 normal merge commit `1777461`，包含 local SB graph fix、remote Apply One Season/dark-mode graphs、combined regression tests、documentation、work logs 和从 merged source 重建的 executable；push 前 fetch 确认为 ahead 3 / behind 0，随后 normal push。
- 修改文件：仅 append 更新 `historyworklog.txt` 和本文件；implementation 和 merged executable 位于 commits `22e3d06` 与 `1777461`。
- 测试或验证结果：最终验证保持 65/65 tests passed；merged EXE SHA-256 为 `6184478E2D626B03B2F7311F2A93BD5D75519696339CF47DDCDFC86CD72A7F34`；`git ls-remote` 返回 `1777461873b27f2e8902768e97032616fa7e12aa`。
- GitHub Timeline：commits `22e3d06`、`136608a` 和 merge commit `1777461` 已成功 push 至 `origin/main`；本条 publication record 将作为 log-only follow-up commit 推送。

## 2026-09-03 19:38:07 -04:00

- 用户 Prompt 摘要：build package。
- 具体动作：使用 PyInstaller 6.22.0 从当前 `main` source clean build 单文件 Windows executable；更新 distribution checksum；将 executable 和 README 复制到 `release/EloLeagueCalculator-v19-Windows-x64`；创建 flat archive `release/EloLeagueCalculator-v19-Windows-x64.zip`；更新 tracked `dist/EloLeagueCalculator.exe`；清理 temporary build directories 和生成的 bytecode changes。
- 修改文件：`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/README.txt`；`release/EloLeagueCalculator-v19-Windows-x64.zip`；`historyworklog.txt`；本文件。
- 测试或验证结果：65 项 unit tests 全部通过；PyInstaller classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；warning report 仅包含预期的 platform-conditional modules；executable 在 isolated smoke test 中持续运行 5 秒，两个 exact v19 executable processes 均已停止。EXE SHA-256 为 `27686893DBEFF3512097506016B6CDBB1871EE4564F86959B79F848DF87F2DD6`；ZIP SHA-256 为 `B8F9CFED8AB9A850CC6977CF74869505439B7B0990C85A236556EB5C495A3C0C`。
- GitHub Timeline：package changes 位于本地且尚未 commit；用户要求 build，未要求 commit 或 push。

## 2026-09-03 20:07:25 -04:00

- 用户 Prompt 摘要：update readme。
- 具体动作：更新 root `README.md` 的 standalone Windows package section，加入 v19 archive path、source commit、archive contents、解压和启动说明、EXE/ZIP SHA-256，以及已完成的 tests 和 launch validation；保留 worktree 中先前存在且与本任务无关的 source/test changes。
- 修改文件：`README.md`；`historyworklog.txt`；本文件。
- 测试或验证结果：README 和 work-log update 通过 `git diff --check`；package verification 保持 65/65 unit tests passed 和 isolated five-second Windows launch test passed；README 中的 hashes 与生成的 v19 artifacts 一致。
- GitHub Timeline：README 和 package changes 仍位于本地且尚未 commit；用户未要求 commit 或 push。

## 2026-09-03 20:06:59 -04:00

- 用户 Prompt 摘要：修复 standings 显示为 Player 1、Player 10、Player 11、Player 12、Player 2 的排序错误。
- 具体动作：将 Elo 和 SB 完全相同时的 alphabetical name fallback 改为稳定的 roster-position ordering；增加 12-player GUI regression test；记录该行为；从修正后的 source 重建 tracked executable 和待提交的 v19 Windows package；删除 temporary build files。
- 修改文件：`elo_calculator.py`；`test_elo_model.py`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/README.txt`；`release/EloLeagueCalculator-v19-Windows-x64.zip`；`historyworklog.txt`；本文件。
- 测试或验证结果：66 项 unit tests 全部通过，包括新的 Player 1 至 Player 12 ordering regression；`git diff --check` 和 Python source ASCII-only checks 通过。PyInstaller 6.22.2 classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；重建的 executable 在 isolated 4-second smoke test 中持续运行。EXE SHA-256 为 `926AA67CC1E19FA17CA0474FC46DA1B4B63138197342736DF51AABA003434281`；ZIP SHA-256 为 `437A493D7402BCB4096AC54B9036FA9BBC699E0AD07C56A6D8C386C369467A73`。
- GitHub Timeline：ordering fix 和更新后的 v19 package 位于本地且尚未 commit；用户未要求 push。随后创建 focused local commit。

## 2026-09-03 20:09:00 -04:00

- 用户 Prompt 摘要：update readme；在 concurrent v19 rebuild 后校正文档。
- 具体动作：README verification 期间检测到 commit `5b5b582` 已使用 tied-standings ordering fix 重建并提交 v19；校正 root `README.md`，写入 commit `5b5b582`、实际 rebuilt EXE/ZIP hashes、66 项 passing tests 和 4-second launch check；此前 append-only record 保留为 rebuild 前的历史状态。
- 修改文件：`README.md`；`historyworklog.txt`；本文件。
- 测试或验证结果：确认两个 tracked executable copies 的 SHA-256 均为 `926AA67CC1E19FA17CA0474FC46DA1B4B63138197342736DF51AABA003434281`；v19 ZIP SHA-256 为 `437A493D7402BCB4096AC54B9036FA9BBC699E0AD07C56A6D8C386C369467A73`；test suite 包含 66 项 tests，rebuild record 报告全部通过；targeted documentation diff check 通过。
- GitHub Timeline：commit `5b5b582` 已在本地创建但尚未 push；本次 README correction 和 logs 也位于本地且尚未 commit。

## 2026-09-03 20:10:00 -04:00

- 用户 Prompt 摘要：local package commit 被 concurrent amend 后完成 README update。
- 具体动作：从 package description 删除可能继续变化的 local commit identifier，使 README 在 unpushed commit 再次 amend 时仍保持准确；保留 verified v19 path、contents、hashes 和 validation results。
- 修改文件：`README.md`；`historyworklog.txt`；本文件。
- 测试或验证结果：EXE 和 ZIP hashes 仍分别为 `926AA67CC1E19FA17CA0474FC46DA1B4B63138197342736DF51AABA003434281` 和 `437A493D7402BCB4096AC54B9036FA9BBC699E0AD07C56A6D8C386C369467A73`；targeted `git diff --check` 通过。
- GitHub Timeline：v19 package commit 仍位于本地且领先 `origin/main`；README correction 和 work-log entries 尚未 commit 或 push。

## 2026-09-03 19:55:00 -04:00

- 用户 Prompt 摘要：build and push。
- 具体动作：使用 PyInstaller 重新构廻可执行程序，包含了自然排序和决背局规则慴改。打包为 v19 压缩包至 `release/EloLeagueCalculator-v19-Windows-x64.zip`.随后将所有更改推送到 GitHub (origin/main)。
- 修改文件：historyworklog.txt；历史工作记录.txt；dist/EloLeagueCalculator.exe；release/EloLeagueCalculator-v19-Windows-x64.zip。
- 测试或验证绒果：PyInstaller 构廻并鉓包 v19 成功。
- GitHub Timeline：推送到 origin/main。

## 2026-09-03 20:02:03 -04:00

- 用户 Prompt 摘要：截图中的 League Settings popup 无法完整显示；当前为 64-player、First-to-10 league。
- 具体动作：将 dialog 初始尺寸限制在 usable screen 范围内；将完整 settings form 放入统一的垂直滚动区域；Cancel 和 Save 固定在始终可见的 footer；增加 mouse-wheel scrolling；移除 multiplier list 的 nested scrollbar，使大型 First-to-N 列表随主表单滚动。
- 修改文件：`elo_calculator.py`；`test_elo_model.py`；`historyworklog.txt`；本文件。
- 测试或验证结果：Python compile 通过；66 项 unit tests 全部通过，包括 screen-size capping regression coverage；focused whitespace check 通过。
- GitHub Timeline：未 commit，未 push；用户本次未要求发布。既有无关 build、release、cache 和 helper-file changes 未纳入本次修改。

## 2026-09-03 20:04:24 -04:00

- 用户 Prompt 摘要：push League Settings popup fit fix。
- 具体动作：创建 focused commit `f056b29`，包含 screen-bounded、scrollable settings dialog、fixed action footer、regression test 和 work records；以 normal push 更新 `origin/main`。Generated build、cache、release 和 helper files 均未纳入提交。
- 修改文件：append-only 更新 `historyworklog.txt` 和本文件；implementation 与 test changes 位于 `f056b29`。
- 测试或验证结果：publication verification 保持 66/66 unit tests passing；focused whitespace check 通过；push 后 local `HEAD` 与 `origin/main` 均为 `f056b2973c6b0a0c887e1c1f35efa5f6a99fd8b9`。
- GitHub Timeline：commit `f056b29` 已成功 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 推送。

## 2026-09-03 20:13:03 -04:00

- 用户 Prompt 摘要：push standings-order fix 和 v19 package。
- 具体动作：fetch `origin/main` 的四个新增 commits；使用 normal merge 合并 remote natural-sort、standings-tiebreaker、package-log 和 screen-bounded League Settings changes，以及 local ordering regression 和 v19 package。Merged regression 发现 remote natural-sort helper 的 digit expression 被错误转义；修正为分割实际 numeric components。保留两份 append-only histories；从 merged source 重建最终 executable 和 v19 ZIP；更新 package hashes；清理 temporary build files。
- 修改文件：`elo_calculator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/README.txt`；`release/EloLeagueCalculator-v19-Windows-x64.zip`；`historyworklog.txt`；本文件。
- 测试或验证结果：67 项 merged unit tests 全部通过，包括 numeric Player 1 至 Player 12 ordering 和 settings-window bounds；`git diff --check`、conflict-marker 和 Python ASCII-only checks 通过。PyInstaller 6.22.2 classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；最终 executable 通过 isolated 4-second launch check，并停止两个 exact-path processes。EXE SHA-256 为 `D45426DE2D59A38430864F54A726A91FFAC4A81D0C30B96CE1ABA43FCEE4EA05`；ZIP SHA-256 为 `CAA56D0A3EDC8896E0F007C1BA0D4B6E733D35713716B0F7E8139F2E9DE0255C`。
- GitHub Timeline：normal merge 已在本地完成 conflict resolution，等待创建 merge commit；用户已明确要求 normal push 至 `origin/main`。

## 2026-09-03 20:14:59 -04:00

- 用户 Prompt 摘要：完成 GitHub push。
- 具体动作：创建 merge commit `6d2aee3`，包含 ordering fix、final v19 package、natural-sort correction、popup-fit changes、tests、documentation 和合并后的 append-only histories。最终 fetch 发现 remote work-log correction `4c2ed49`，以 `e715bac` 正常 merge；随后不重写历史地 push `main`。
- 修改文件：本次仅 append 更新 `historyworklog.txt` 和本文件；implementation 和 package changes 位于 commits `7bc6afe`、`6d2aee3` 和 `e715bac`。
- 测试或验证结果：最终验证保持 67/67 tests passing；final EXE SHA-256 为 `D45426DE2D59A38430864F54A726A91FFAC4A81D0C30B96CE1ABA43FCEE4EA05`；final ZIP SHA-256 为 `CAA56D0A3EDC8896E0F007C1BA0D4B6E733D35713716B0F7E8139F2E9DE0255C`；push 后 `git ls-remote` 返回 `e715bac4f7b30be0792a297fc9d6d53eb6282a2c`。
- GitHub Timeline：截至 `e715bac` 的 commits 已成功 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 提交并 push。

## 2026-09-03 20:16:51 -04:00

- 用户 Prompt 摘要：在 concurrent package verification changes 后完成 push。
- 具体动作：main code push 后检测到两个 tracked executable copies 和 v19 ZIP 被 concurrent rebuild；保留该 rebuild，核对 documented hashes 与实际文件，按实际 merged suite 将 validation count 校正为 68 tests，并独立 smoke-test rebuilt tracked executable。
- 修改文件：`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/README.txt`；`release/EloLeagueCalculator-v19-Windows-x64.zip`；`historyworklog.txt`；本文件。
- 测试或验证结果：68 项 unit tests 全部通过；`git diff --check` 通过；final tracked executable 在 isolated 4-second launch check 中持续运行。两个 executable copies 的 SHA-256 均为 `EAAE3E046D45842B8D281FF11F76900E6C4202C1F056A116B187792EA2FD475E`；v19 ZIP SHA-256 为 `139D86ECE887C74C99247AEC356A49E53C346A5AC5ABF43D508D4C820328A10A`。
- GitHub Timeline：截至 `e715bac` 的 main source 和 merge commits 已 push；final package refresh 和 publication records 位于本地，将 normal commit 并 push。

## 2026-09-03 20:18:00 -04:00

- 用户 Prompt 摘要：review code、patch defects，并 commit 两组 integrated features。
- 具体动作：审查 merged natural-order/tiebreaker 和 screen-bounded League Settings changes；保留两组功能；验证 corrected numeric regex；修复 standings documentation stale defect；增加覆盖 Elo、match score percentage、SB、game percentage 和 natural-name 完整 tiebreak chain 的 regression test；同步 root/package READMEs；从 reviewed source 重建 v19；删除误纳入版本控制的 PyInstaller intermediate directories。
- 修改文件：`elo_calculator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v19-Windows-x64/README.txt`；`release/EloLeagueCalculator-v19-Windows-x64.zip`；`historyworklog.txt`；本文件。Generated `build_review_merge_final` 和 `dist_review_merge_final` directories 将删除。
- 测试或验证结果：Python compilation 和 68 项 unit tests 全部通过；conflict-marker 和 whitespace checks 通过；PyInstaller 6.22.0 classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；仅保留预期 platform-conditional warnings；final EXE 通过 isolated five-second launch test，两个 exact-path processes 均已停止。EXE SHA-256 为 `EAAE3E046D45842B8D281FF11F76900E6C4202C1F056A116B187792EA2FD475E`；ZIP SHA-256 为 `139D86ECE887C74C99247AEC356A49E53C346A5AC5ABF43D508D4C820328A10A`。
- GitHub Timeline：此前 integrated merge 已 push 至 `e715bac`；本次 review/package cleanup 位于本地，将根据用户仅要求 commit 而不 push。

## 2026-09-03 20:19:06 -04:00

- 用户 Prompt 摘要：push reviewed code 和 final v19 package。
- 具体动作：fetch `origin/main`，确认 reviewed publication commit 已存在于 remote，并在追加本条 append-only publication record 前验证 GitHub `main` 的 exact commit。
- 修改文件：`historyworklog.txt`；本文件。
- 测试或验证结果：publication verification 保持 68/68 tests passing；EXE SHA-256 为 `EAAE3E046D45842B8D281FF11F76900E6C4202C1F056A116B187792EA2FD475E`；ZIP SHA-256 为 `139D86ECE887C74C99247AEC356A49E53C346A5AC5ABF43D508D4C820328A10A`；`git ls-remote` 返回 `d87a2c4173795604bf8ea4fa072a532fc7536ff9`。
- GitHub Timeline：commit `d87a2c4` 已 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 提交并 push。

## 2026-09-03 20:28:04 -04:00

- 用户 Prompt 摘要：增加 head-to-head 作为 standings secondary tiebreaker，push 并生成 v20。
- 具体动作：为每个 equal-Elo group 增加 reusable head-to-head match-score calculation，win 计 1 point、draw 计 0.5；将其放在 Elo 之后、overall Match %、SB、Game % 和 natural player-name order 之前；增加 regression coverage，证明 head-to-head 优先于较高的 overall match percentage；更新 documentation；构建 tracked executable 和独立 flat `release/EloLeagueCalculator-v20-Windows-x64.zip`，同时保留 v19；清理 temporary build/cache files。
- 修改文件：`elo_model.py`；`elo_calculator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v20-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v20-Windows-x64/README.txt`；`release/EloLeagueCalculator-v20-Windows-x64.zip`；`historyworklog.txt`；本文件。
- 测试或验证结果：69 项 unit tests 全部通过；`git diff --check` 和 Python ASCII-only checks 通过。PyInstaller 6.22.2 classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；v20 executable 通过 isolated 5-second launch check。Archive 仅包含 `EloLeagueCalculator.exe` 和 `README.txt`。EXE SHA-256 为 `4BC4C422F437503663C6D673A8F4F9CCB194AC8D70BC23815681672F03150DF6`；ZIP SHA-256 为 `6EBD27780D5D330D247BDA0C684F129B1764376D40B0E7CA3A1CA3B73B709554`。
- GitHub Timeline：head-to-head implementation 和 v20 package 位于本地且尚未 commit；用户已明确要求 commit 并 normal push 至 `origin/main`。

## 2026-09-03 20:29:58 -04:00

- 用户 Prompt 摘要：完成 head-to-head v20 GitHub publication。
- 具体动作：创建 commit `9e94a29`，包含 head-to-head secondary tiebreaker、regression test、documentation、tracked executable 和 v20 archive；publication 前立即 fetch，确认 ahead 1 / behind 0，并以 normal push 发布，不重写历史。
- 修改文件：本次仅 append 更新 `historyworklog.txt` 和本文件；implementation 和 v20 artifacts 位于 commit `9e94a29`。
- 测试或验证结果：publication verification 保持 69/69 tests passing；EXE SHA-256 为 `4BC4C422F437503663C6D673A8F4F9CCB194AC8D70BC23815681672F03150DF6`；ZIP SHA-256 为 `6EBD27780D5D330D247BDA0C684F129B1764376D40B0E7CA3A1CA3B73B709554`；`git ls-remote` 返回 `9e94a29a7b8f0aafe86edabcedc6aec9a10d7c8e`。
- GitHub Timeline：commit `9e94a29` 已成功 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 提交并 push。

## 2026-09-07 14:22:59 -04:00

- 用户 Prompt 摘要：review code、patch defects、package，并 push 到 GitHub。
- 具体动作：审查 model、storage、simulator、UI integration 和最近的 head-to-head change；复现 deterministic seven-player season，其中 simulator 在两个 match-win leaders 之间错误平分 title，虽然其中一人直接击败另一人；在 simulated-season ranking 中将 head-to-head score 加到 match wins 之后；增加 regression coverage；更新两份 distribution documents；保留 v20 并建立独立 v21 Windows package；清理 temporary build 和 bytecode files。
- 修改文件：`elo_simulator.py`；`test_elo_model.py`；`README.md`；`DISTRIBUTION_README.txt`；`dist/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v21-Windows-x64/EloLeagueCalculator.exe`；`release/EloLeagueCalculator-v21-Windows-x64/README.txt`；`release/EloLeagueCalculator-v21-Windows-x64.zip`；`historyworklog.txt`；本文件。
- 测试或验证结果：Python compilation 和 70 项 unit tests 全部通过；conflict-marker 和 whitespace checks 通过。PyInstaller 6.22.0 在 Python 3.12.14 上 classified 1,003 runtime entries 并包含 `pyi_rth__tkinter.py`；仅保留预期 platform-conditional warnings；v21 executable 通过 isolated five-second launch test，两个 exact-path processes 均停止。EXE SHA-256 为 `7C13201EE3A81D7923909A0BC6BCA4D7A720D83BCD102AF293CD3C81D3A6C994`；ZIP SHA-256 为 `4223B2A577BC62F0CF858EDA60E8FC2742F823B7EC83CCCE037EB8C9D3415062`。
- GitHub Timeline：changes 位于本地且尚未 commit；用户已明确要求 normal push 至 `origin/main`。

## 2026-09-07 14:24:51 -04:00

- 用户 Prompt 摘要：完成 reviewed v21 GitHub publication。
- 具体动作：创建 commit `4962704`，包含 simulator head-to-head fix、regression test、updated documentation、tracked executable 和独立 v21 archive；publication 前立即 fetch，确认无 divergence，并以 normal push 发布，不重写历史。
- 修改文件：本次仅 append 更新 `historyworklog.txt` 和本文件；implementation 和 v21 artifacts 位于 commit `4962704`。
- 测试或验证结果：publication verification 保持 70/70 tests passing；EXE SHA-256 为 `7C13201EE3A81D7923909A0BC6BCA4D7A720D83BCD102AF293CD3C81D3A6C994`；ZIP SHA-256 为 `4223B2A577BC62F0CF858EDA60E8FC2742F823B7EC83CCCE037EB8C9D3415062`；`git ls-remote` 返回 `496270466f14f5a1d6bcb7e32ea6a07b5c6a99d0`。
- GitHub Timeline：commit `4962704` 已成功 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 提交并 push。

## 2026-09-07 16:16:06 -04:00

- 用户 Prompt 摘要：push dynamic League Settings length fix。
- 具体动作：在十个更新的 v19-v21 commits 后将 change 集成至 current `origin/main`；移除 remaining fixed 760-pixel target；League Settings 根据 visible content 计算 size，并在 format 或 games-to-win 变化时动态 resize/recenter；保留 screen capping、scrolling 和 fixed Save/Cancel controls。
- 修改文件：`elo_calculator.py`；`test_elo_model.py`；`historyworklog.txt`；本文件。
- 测试或验证结果：Python compilation 和 70 项 unit tests 全部通过；focused whitespace check 通过；dynamic-height 和 screen-cap regression coverage 通过。
- GitHub Timeline：integrated changes 位于基于 current `origin/main` 的本地 `codex/dynamic-popup` branch，尚未 commit；用户要求 normal push。

## 2026-09-07 16:17:37 -04:00

- 用户 Prompt 摘要：完成 dynamic League Settings popup push。
- 具体动作：从 latest v21 branch 创建 focused commit `a662745` 并以 normal push 更新 `origin/main`；existing release packages 和无关 generated files 均未纳入提交。
- 修改文件：append-only 更新 `historyworklog.txt` 和本文件；implementation 与 test changes 位于 `a662745`。
- 测试或验证结果：publication verification 保持 70/70 tests passing；push 后 local integration `HEAD` 与 `origin/main` 均为 `a662745417a5cfd4c52a49abc917402a0c2dbace`。
- GitHub Timeline：commit `a662745` 已成功 push 至 `origin/main`；本 publication record 将作为 log-only follow-up commit 推送。
